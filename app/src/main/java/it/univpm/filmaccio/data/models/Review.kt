package it.univpm.filmaccio.data.models

// Data class che potrebbe essere utile per memorizzare i dati di una recensione di un film/serie TV
// Ho pensato che ci potrebbe tornare utile in futuro e quindi per ora l'ho inserita.
data class Review(val id: Int)